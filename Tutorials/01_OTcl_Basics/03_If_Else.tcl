set a 11

# two ways to do if, but brace stays on the same line as if or else
if {$a > 10} {
puts "a is greater than 10"
} else {
puts "a is less than or equal to 10"
}

# Other way by if [expr $a > 10] { ... }
